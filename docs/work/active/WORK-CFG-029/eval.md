# Oracles

## O que prova corretude
Small real valid Publication: sufficient → STRUCTURALLY_VALID/CFG_BUILT; restricted → typed RESOURCE_LIMIT, INCOMPLETE_VALIDATION, traversalCompleted=false, no graph; repeat sufficient succeeds. Composition throws EXTERNAL_RESOURCE_LIMIT before session/solver/result. CLI real limited AirJson → exit 7, operational stderr, no result/receipt/delivery. W1–W5, CP4E twice, CP3, overwrite and exact memory/file comparison.

## Casos adversariais
INVALID_IR precedence including omitted diagnostics; RESOURCE_LIMIT distinct from VALIDATION_LIMIT, generic incomplete, capability and projection unsupported; no graph for every failure. Mutants: generic fallthrough, unsupported, invalid, product, collapse to validation limit, generic-as-resource, source-open, session start, CLI unsupported. Compile/RED/restore/second GREEN required. No physical OOM.
