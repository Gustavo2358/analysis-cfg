id: WORK-STORAGE-CFG-001
title: Storage Semantics campaign — ST-W0..ST-W5
status: IN_PROGRESS
scope:
  - Promote storage/RD/value residuals of BACKLOG-CFG-020 and BACKLOG-DF-001/004/003 handoffs.
  - ST-W0 baseline/contracts/oracles; ST-W1 control transport integration and explicit codecs.
  - ST-W2 resolver/canonical effects/interval RD/query/replay using existing solver.
  - ST-W3/W4 regional values and dependency projection; ST-W5 fragments/copy/joins/context/provenance.
  - Cumulative FAST, E2E, adversarial/mutation and final local corpus qualification; no W6-W8 or merge.
notes: One campaign branch and persistent draft PR, with human review after qualified W5 only.
design_w1:
  authority: AIR codec extension 12 at a928791; air-java model/validator/JSON at 3feccca.
  change: Exact IBM1047 capability admission for sequential control after validator preflight; original operations retained.
  oracle: Manual regional wire, CopyBytes followed by computed CALL and Return, independent topology and concrete byte expectation.
  boundaries: No storage/value analysis in BuildCfg; other capability names or versions remain unsupported.
design_w2:
  contract: docs/domain/regional-analysis.md
  ownership: analysis-kernel/storage prepares locations and normalized effects; analysis-kernel/rd consumes the existing solver SPI.
  progress: Interval RD, entry events, stable batched before/after/outcome queries and file traversal have independent oracle coverage; cumulative W2/M0 qualified at ec28ba125e3d2490f2ad78ae72225bd89e3ef849.
  boundaries: No source language interpretation, new solver or consumer-side repair; explicit bases, proofs, alternatives, unknown tails and outcomes.
design_w3:
  contract: docs/domain/regional-values.md
  progress: Regional image foundation, explicit provider and CALL-before-Invoke projection have focal and complete repository FAST coverage; real source E2E and cumulative M1 qualification remain pending.
  oracle: Independent octet examples and 48 full-string control graphs with 240 observations; displaced view, unknown prefix, alias effects, scalar mixture, support convergence and integral copy capture.
  boundaries: Existing solver/replay reused; historical scalar profiles and wire preserved; multi-producer projection keeps FRAGMENT_COMPOSITION_PENDING until W5; no regional-initializer validator expansion.
design_w4:
  progress: M1 qualified; W4 producer/reader pins updated to SP 2.8.0/storage 1.1.0 with component relations and localized eligibility.
  integration: Seventeen independent source recipes cover both overlay directions, larger/smaller footprint, nested FILLER, disjoint sibling/half, descending internal levels, copybook, 1/2/5/40 same-name components, scalar INT mixture and two conservative negatives.
  gates: Development E2E and repository FAST passed after correcting two fixture/adapter errors; cumulative A/B, inventory permutations and mutations remain required before M2.
  boundaries: Generic AIR analysis unchanged; same bytes with different codecs remain different interpretations. Multi-producer candidate composition remains pending W5.
design_w5:
  contract: docs/domain/regional-values.md
  progress: M2 qualified in the campaign receipt; W5.1 joint image domain and exclusive destination effects under focal verification.
  oracle: Five independent composition/correlation/overlap tests first failed semantically, then passed; join laws and support-only equality also passed.
  boundaries: RegionalValues 2 and regional-text-images@2 identify the extended domain; RegionalValueFact@1 and legacy scalar wire remain compatible. No path witness or arbitrary candidate cap.
