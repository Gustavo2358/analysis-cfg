# EP-W2 qualification

Status: GREEN for centralized update authority. Campaign W3–W5 pending.
Production head: `59aaebdd3b72c379f60c1672f0d253c72e3a45b7`. Only analysis-cfg production changed in this wave.

## Proved

RD, correlated Regional Values and scalar values now route strong replacement
through KillAuthority permits. Unknown/MAY APIs preserve old alternatives and
supports. Permits require required execution, known complete destinations and
direct target evidence. Possible alias effects cannot acquire kill authority.
Closed finite destination choices retain their existing per-alternative concrete
semantics; an open selection retains the unchanged store. No new solver.

## Counterproof executed

Five independently installed policy mutants all failed semantic assertions:
UNKNOWN→clear, MAY→kill, unproved alias→kill, query reseed and unknown storage→empty.
The versioned script restores each source in finally and rejects compiler/setup
failures as qualification. Logs, exact patches, source hashes and JUnit failures:
`w2/policy-mutants-01/results.json` and sibling files in campaign evidence.
No mutant remains installed; unmutated classes were rebuilt before product checks.

## NEW / REUSED / NOT RERUN

NEW: four generic value laws, three authority obligation tests, five targeted
mutations, focused scalar/RD/regional/Choice controls, `w2/cfg-fast-01.log` PASS
(452 technical tests plus required Python/architecture gates). Single FAST closure.
Three changed-consumer E2Es reused immutable W1 source/SP inputs: unknown profile,
unknown allocation and exact overwrite. Source support, AIR entry, RD BEFORE,
Regional Values BEFORE, DependencySite and JSON passed; see
`w2/entry-boundaries-01.json`. Exact overwrite contains only PROGB.
REUSED: W1 producer/lower/AIR qualifications and historical witness evidence.
NOT RERUN: unrelated repo FAST, full/corpus/Storage/CICS/DVI25 campaigns.
Compiled inventory updates reflect inspected KillAuthority dependencies and APIs;
no expected semantic output was regenerated.

## Limits and independent causes

This wave proves update authority for the admitted operation/storage domains.
It does not prove every upstream unknown construct is correctly bounded, every
mixed physical/logical copy is modeled, entry facts always coexist, or semantic
preparation failures are contained. Those independent causes can still remove
product evidence before or after a transfer and belong to W3–W5.
LOCAL FIX / bounded ARCHITECTURAL LAW: proved. INCIDENT CLASS: pending.
REAL CASE = NOT AVAILABLE. No 4/4 claim. GO to W3.
