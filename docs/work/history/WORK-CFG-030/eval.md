# Evaluation

## O que prova corretude
All Java/POM and fixture bytes equal merged PR13. Normative contract unchanged; CP4E preserves PROGA, actual Assign producer/origin, model=false/source=true/effective=true. Fresh A/B SP/AIR/result bytes equal; memory/file semantic and result bytes equal. CP3 complete with zero analyses/batches/consumers/facts. Generic overwrite ends NEWER supported by last Assign.

## Casos adversariais
Missing stages, stale JAR, changed historical evidence or Java, moved source pin, lost support, remainder drift and nondeterminism fail. Existing full challenges remain required; no new mutant campaign needed.
