# Harness remediation

## Problema
Registry adversarial could construct blocked→blocked. Later independent W5 RED included JVM stderr in javap product. Prior STOPs remain investigation history.

## Objetivo
Prove mismatch before validation; canonical tool value is stdout only, stderr remains diagnostic, nonzero exit fails with both streams. Verify main and the same frozen candidate without weakening inventory.

## Fora de escopo
Production semantics, new AIR operations, CALL, CP6 implementation and expected inventory regeneration.
