# Plan

## Fatiamento
Retain registry RED/GREEN; capture RED then stream correction and GREEN; main gates; frozen candidate gates; review, CI and harness merge. Final synchronization follows on a separate branch.

## Dependências
Frozen candidate receipt and exact upstream sources; Java 21, Maven isolated dependencies; existing approved inventory and semantic contracts.
