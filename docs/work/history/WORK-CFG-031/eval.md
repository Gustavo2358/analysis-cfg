# Evaluation

## O que prova corretude
Both matched registry controls and both constructed mismatches. Real subprocess stdout equality, visible arbitrary stderr and nonzero failure. Real compiled W5 inventory exact equality with injected diagnostics and rejection of changed descriptor. Main and same frozen candidate canonical gates.

## Casos adversariais
Preserved constant-blocked mutant RED and restore. Diagnostic stream RED/GREEN; stdout resembling warnings retained; fatal stderr preserved; descriptor mutation still rejected. No expected inventory regeneration.
