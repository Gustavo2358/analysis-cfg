# ADR-0007 — Java 21 e modelo AIR compartilhado via air-java

Status: `accepted`. Data: 06/09/2026. Implementação: foundation/preflight em
WORK-CFG-002, porta `BuildCfg` em WORK-CFG-003 e CFG-FIRST em WORK-CFG-005.

## Contexto

O consumer precisa compartilhar o contrato Java com produtores/lowerers sem
duplicar a AIR. A inspeção de `air-java/main` confirmou um modelo imutável,
transport-independent e um validator estático alinhados à Analysis IR 2.0.0.

## Decisão

`analysis-cfg` adotará Java 21, Maven e nenhum recurso preview. O owner físico do
modelo Java AIR é `Gustavo2358/air-java`, com pin ativo
`b78f4068d8a479f48eb048b8d76fa60a0997dc4a` (PR #5 / 1A), coordenadas declaradas
`io.github.gustavo2358:air-java:0.1.0-SNAPSHOT`.

O kernel depende desse artefato e recebe sua
`io.github.gustavo2358.air.model.Publication`. Este repo não implementa cópias de
`Publication`, `Unit`, `Entry`, `Sequence`, `Operation`, `Instruction`,
`Terminator`, `TypeRef`, IDs, `Premise`, `DomainProofScope` ou outros tipos AIR.
`AirValidator` do mesmo artefato executa o preflight estrutural; os evals CFG
continuam independentes.

O modelo reconciliado não possui `Publication.contracts[]`. `ContractRef` é valor,
e os fatos externos são materializados por site de `invoke`; isso reforça que o
consumer recebe um snapshot fechado e não consulta uma autoridade para completá-lo.

A versão `0.1.0-SNAPSHOT` é da biblioteca; `2.0.0` é da semântica AIR. O SHA fixado
torna a baseline inspecionada imutável. Como não há tag/release observada, o work
item de bootstrap instala temporariamente um checkout detached desse SHA em um
repositório Maven vazio/isolado antes de compilar o consumer. CI confirma o `HEAD`
e executa a instalação a partir da raiz do upstream. Não há fonte/JAR vendorizado,
publicação de artefato nem SNAPSHOT flutuante aceito silenciosamente.

## Alternativas rejeitadas

Java 17 por alinhamento incidental com o frontend; modelo AIR local ou bifurcado;
DTOs paralelos no lowerer/CFG; serialização JSON intermediária obrigatória; validator
AIR reimplementado pelo consumer.

## Consequências e validação

Uma biblioteca compilada com `--release 17` pode ser consumida numa JVM 21; não é
necessário alterar `proleap-poc` neste projeto. O gate do bootstrap verifica target 21,
origem do tipo `Publication`, ausência de tipos AIR duplicados e de dependências de
transporte/frontend no kernel.

A instalação controlada é provisória. Release versionada ou reactor Maven podem
substituí-la sem alterar o core nem a assinatura baseada em `Publication`.

## Evidência

Evidência histórica da decisão em 06/09/2026: POM, API, documentação,
172 checks determinísticos e CI `contracts` do `air-java` em
`6a4091e5394fc22b3d2ada9abbdb530eb3572a58`; Analysis IR 2.0.0 no commit normativo
`122ce54e1b9ef9b00646f93ece409ca8b63bc933`. Ver [fontes](../../sources/index.md) e
[toolchain](../../engineering/toolchain-and-modules.md).

A atualização de proveniência em 07/09/2026 mantém esta decisão: o modelo/validator
está em `air-model/` (artefato `air-java`), e o codec compartilhado 1A em
`air-json/`. O kernel continua dependendo apenas de `air-java`. [Pin e paths atuais](../../sources/sources.lock.json).
