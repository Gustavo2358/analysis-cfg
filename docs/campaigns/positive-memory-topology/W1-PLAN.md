# POSITIVE_MEMORY_TOPOLOGY — W1 plan

Status: IN_PROGRESS. W1 authorized after accepted W0-R1. W2/W3 not started.

## CP0 and invariant

Anchor starts at `a2bd04b9d106c3abe0250edccc1aad7e9e386ca8` (PR #45 DRAFT), over CFG #43 `98fa57c3db2edf9f70bb7a99bb667dbf36d28104`. Remote parent heads unchanged: frontend #56 `edb64520a6269be9fa6d71cd47e6974112fbfece`, lower #32 `f8e181f95929c650181c989318f8ba23d1e68a1a`, AIR #20 `646ca3ab1687d43f7d2063fc2a8f3837ab3cf9fa`; IR main `3fff18e2c16663a3f599207457caa1946d2e0945`. Isolated campaign worktrees preserve existing work and historical evidence. No companion PR existed at CP0.

LOGICAL_ONLY remains CLI/API default. Physical execution requires existing `--experimental-physical` / `StorageAnalysisMode.EXPERIMENTAL_PHYSICAL`, remains EXPERIMENTAL / NOT PRODUCTION QUALIFIED. No fallback, merge or operational unbanning.

## Representation checkpoint

Reuse StorageId, Cell/Region, bindings and ranges. Distinct bases are independent state identities; sharing is represented positively by same-base views, aliases or choices. No generated matrix of DisjointStorage premises. Preserve validation of existing explicit premises and all bounds/types/references; a redundant assertion need not be removed from the wire to remove its obligation.

The vertical fixtures retain a known PIC X(n) representation while an additional unmodeled representation clause is diagnosed. The abstraction uses known textual width, not the omitted clause's concrete physical layout. A family `01 SRC; 05 PREFIX PIC X(3); 05 SUFFIX PIC X(5) <omitted clause>` has an eight-character root and member slices; copying to an eight-character family partitioned as 4+4 uses root text/byte correspondence, never member names. After copy, changing SRC does not change DST. Supported REDEFINES/RENAMES continue sharing the same base. No zero width or numeric-to-text rule is invented. Unrepresented types must not receive invented codecs; nominal coverage is distinct from executability of an AIR operation.

The producer's existing Coverage/Evidence/Precision diagnostics remain publication metadata. Semantic uncertainty is represented by executable Unknown/Choice/bounded effects. Coverage metadata alone never determines targets, force, control, state equality, captures, provider mode or effective value remainder. True unsupported AIR operations remain refusal/errors. Changes to producer projection and consumer interpretation ship together with exact pins.

CALL without body knowledge retains target/site/known operands and normal continuation of the supported abstraction; missing implementation does not justify global foreign writes. Real modeled unknown input/return and explicit AIR broad effects remain executable.

## Executable oracles and cohorts

T1 mode/default and key isolation; T2 independent/same-family representation gap; T3 positive bases plus same-base overlap; T4 partition-independent copy/snapshot/correlation; T5 scalar and regional 0/1/50 diagnostic mutations; T6 real unknown MUST/MAY versus omitted transformation; T7 prior external CALL followed by computed CALL BEFORE; T8 invalid AIR and existing resource/control regressions.

Freeze baseline logical synthetic fixtures and classpath hashes before production edits. Preserve raw outputs and failures. A/B use identical source/settings and old/new runtime; C/D consume identical physical AIR bytes with and without opt-in. Frontend physical profile is `ibm-enterprise-6.4-fixed-display-1047@1`, distinct from consumer mode. D must show positive physicalGroupsApplied/physicalWritesApplied and a supported physical observation; B/C show zero physical work.

Measure independent bases/writes at 4/5, 16/50 and 32/100 without disjoint premises, separating preparation/solve/replay and historic Event positions. Existing W0 numbers are historical controls only. No corporate source or qualification claim.

## Ownership and checkpoints

Frontend, lower, and normative AIR boundaries can be edited independently under the representation above; anchor owns consumer, integrated fixtures, pins and reports. No overlapping edits. Pin synchronization follows immutable companion commits. CP1 RED characterization precedes each production change; CP2 implements; CP3 focal GREEN, regression and physical measurements; CP4 mandatory FAST and remote checks at final SHAs. W1-REPORT records all mechanisms M01–M21 and remaining W2 work. This plan is an internal checkpoint, not a request for another authorization.
