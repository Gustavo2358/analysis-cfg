# GO TO DEPENDING ON qualification

Baseline: real integrated merges after PERFORM FAMILY, recorded in
[the pins](carddemo-goto-depending-pins.json). Source/build equivalence to the
preserved historical Full is checked separately from merge identity.

Production strategy: SP 2.6 ordered occurrence facts lower to one existing generic
AIR Opaque envelope with finite alternatives and a separate control remainder.
AIR and CFG production: NONE. PossibleValues lattice, fixed point, RD and numeric
analysis: unchanged. Decisions read the integer selector once and produce no values.

The local runner `scripts/project/e2e_goto_depending.py` has 33 source fixtures:
D1–D12, a cycle, seven valid cardinalities plus the parser-tolerance 256 case,
BASIC/range incoming and escape/closure, section/ambiguous/empty/foreign targets,
ALTER, unavailable fallthrough and a TEXT selector. Independent oracles check
ordered source spans, typed identities, explicit entry/continuation provenance,
exact known CFG successors and real value-producer support. A/B bytes cover
SP, AIR, CFG and dependency; physical statement/field/sequence permutations
preserve the semantic ordinal list. They never reorder destination semantics.

Frontend tests additionally count actual indexed target resolution attempts;
lower tests check admission-work growth, 1/2/5/40 occurrence multiplicity,
malformed wire/in-memory rejection and canonical entry consistency. The permanent
Finite Multiplicity architecture invariant forbids a smaller product count cap.

Execution order: focused/adversarial, previous 23 GO TO + 18 EVALUATE + 89 PERFORM
and 38 historical cases, affected cohort derived from preserved SP/AST, then
exactly one final local CardDemo Full. No remote Full. Raw files stay outside Git;
final small results and immutable execution pins are appended after observation.

Incomplete proof remains partial: sections, unresolved selector/destination,
unavailable executable entry/continuation, ALTER and above-profile input do not
receive precise closed control. No numeric selector pruning is implemented.

decisionPolicy = HUMAN; rankingAuthority = ADVISORY_ONLY. No merge authorized.

## Final execution

All planned gates passed on the exact clean snapshots in the pins.
Final counts: focal/adversarial 33; GO TO 23; EVALUATE 18; PERFORM 89; historical 38.
The affected population is zero. One final local Full preserved all 73 CALL vectors
and all six input blockers. See [the measured delta](carddemo-after-goto-depending.md)
and [machine-readable qualification](goto-depending-qualification.json).
No AIR/CFG/PossibleValues production change or merge occurred.
