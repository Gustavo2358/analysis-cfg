       IDENTIFICATION DIVISION.
       PROGRAM-ID. SORTIO.
       ENVIRONMENT DIVISION.
       INPUT-OUTPUT SECTION.
       FILE-CONTROL.
       SELECT S ASSIGN TO SORTWK.
       SELECT A ASSIGN TO INA.
       SELECT B ASSIGN TO INB.
       SELECT C ASSIGN TO OUTC.
       DATA DIVISION.
       FILE SECTION.
       SD S.
       01 SR.
        02 SK PIC X(4).
        02 SK2 PIC X(4).
       FD A.
       01 AR PIC X(8).
       FD B.
       01 BR PIC X(8).
       FD C.
       01 CR PIC X(8).
       WORKING-STORAGE SECTION.
       01 BUF PIC X(8).
       PROCEDURE DIVISION.
       MOVE 'STALE001' TO BUF.
       SORT S ON ASCENDING KEY SK USING A
        OUTPUT PROCEDURE OUT-P.
       GOBACK.
       OUT-P.
       RETURN S INTO BUF AT END CONTINUE
        NOT AT END CALL BUF END-RETURN.
       END PROGRAM SORTIO.
