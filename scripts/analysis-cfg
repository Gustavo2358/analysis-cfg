#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
exec java -cp "$ROOT/cfg-launcher/target/classes:$ROOT/cfg-launcher/target/dependency/*" \
  io.github.gustavo2358.analysis.cfg.launcher.AnalysisCfg "$@"
